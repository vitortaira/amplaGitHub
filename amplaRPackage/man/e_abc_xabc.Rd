% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/e_abc_xabc.R
\name{e_abc_xabc}
\alias{e_abc_xabc}
\title{Extract and Tidy Data from ABC Bank Statement Excel Files}
\usage{
e_abc_xabc(f_caminho.arquivo_c)
}
\arguments{
\item{f_caminho.arquivo_c}{The full path to the \code{.xlsx} file containing the
ABC bank statement.}
}
\value{
A \code{tibble} with the following columns:
\itemize{
\item \code{data}: Date of the transaction.
\item \code{valor}: The transaction amount.
\item \code{saldo}: The balance after the transaction.
\item \code{descricao}: A description of the transaction.
\item \code{empresa}: The client's name.
\item \code{cnpj}: The client's CNPJ.
\item \code{agencia}: The bank branch number.
\item \code{conta}: The bank account number.
\item \code{periodo.inicio}: The start date of the statement period.
\item \code{periodo.fim}: The end date of the statement period.
\item \code{data.consulta}: The date and time the statement was generated.
\item \code{arquivo}: The original file path.
\item \code{banco}: The name of the bank (extracted from the file).
\item \code{documento}: The transaction document number.
\item \code{operacao}: The operation code or type.
}
}
\description{
This function reads a standardized bank statement from Banco ABC,
which is provided as an Excel file (.xlsx). It extracts both the transaction
data and key metadata from the header, tidies it, and returns a single,
well-structured tibble.
}
\examples{
\dontrun{
# Define the path to your ABC bank statement file
file_path <- "path/to/your/extrato_abc.xlsx"

# Extract and process the data
abc_statement_data <- e_abc_xabc(file_path)

# View the first few rows of the tidy data
print(head(abc_statement_data))

# Perform further analysis, for example, summarizing transactions by day
daily_summary <- abc_statement_data \%>\%
  group_by(data) \%>\%
  summarise(total_valor = sum(valor, na.rm = TRUE))
print(daily_summary)
}
}
