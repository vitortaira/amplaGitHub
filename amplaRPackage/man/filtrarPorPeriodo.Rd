% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filtrar_por_periodo.R
\name{filtrarPorPeriodo}
\alias{filtrarPorPeriodo}
\title{Filtrar Dados por Período}
\usage{
filtrarPorPeriodo(
  dados,
  coluna_data,
  data_inicial = NULL,
  data_final = NULL,
  formato_data = "auto",
  incluir_na = FALSE
)
}
\arguments{
\item{dados}{Um data.frame ou tibble contendo os dados a serem filtrados}

\item{coluna_data}{String. Nome da coluna que contém as datas}

\item{data_inicial}{Date ou string. Data inicial do período (inclusiva).
Se NULL, não aplica filtro de data mínima}

\item{data_final}{Date ou string. Data final do período (inclusiva).
Se NULL, não aplica filtro de data máxima}

\item{formato_data}{String. Formato da data se as datas estiverem como
caracteres. Padrão: "auto" (detecta automaticamente)}

\item{incluir_na}{Logical. Se TRUE, inclui registros com NA na coluna de data.
Padrão: FALSE}
}
\value{
Retorna um data.frame ou tibble filtrado contendo apenas os registros
dentro do período especificado
}
\description{
A função \strong{filtrarPorPeriodo()} filtra um conjunto de dados baseado em um
intervalo de datas especificado. Suporta diferentes formatos de data e
validações robustas.
}
\details{
Esta função oferece flexibilidade para filtrar dados usando:
\itemize{
\item Datas específicas (data_inicial e data_final)
\item Períodos predefinidos (ano corrente, últimos N meses, etc.)
\item Validação automática de formatos de data
\item Tratamento de valores ausentes
}

A função utiliza a biblioteca lubridate para manipulação robusta de datas
e dplyr para operações eficientes de filtro.
}
\examples{
\dontrun{
# Criar dados de exemplo
dados_exemplo <- data.frame(
  id = 1:10,
  data_transacao = seq(as.Date("2024-01-01"), 
                      as.Date("2024-12-31"), 
                      length.out = 10),
  valor = runif(10, 100, 1000)
)

# Filtrar por período específico
dados_filtrados <- filtrarPorPeriodo(
  dados = dados_exemplo,
  coluna_data = "data_transacao",
  data_inicial = "2024-06-01",
  data_final = "2024-12-31"
)

# Filtrar apenas com data inicial
dados_recentes <- filtrarPorPeriodo(
  dados = dados_exemplo,
  coluna_data = "data_transacao",
  data_inicial = "2024-09-01"
)

# Filtrar últimos 3 meses
dados_3meses <- filtrarPorPeriodo(
  dados = dados_exemplo,
  coluna_data = "data_transacao",
  data_inicial = Sys.Date() - months(3),
  data_final = Sys.Date()
)
}

}
\seealso{
\code{\link{calcularPeriodo}}, \code{\link{validarData}}
}
