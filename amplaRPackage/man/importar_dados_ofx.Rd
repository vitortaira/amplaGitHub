% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ofx.R
\name{importar_dados_ofx}
\alias{importar_dados_ofx}
\title{Importa dados de transações de um arquivo OFX de forma robusta}
\usage{
importar_dados_ofx(caminho_arquivo_ofx)
}
\arguments{
\item{caminho_arquivo_ofx}{O caminho completo para o arquivo .ofx que você deseja importar.}
}
\value{
Um \code{tibble} contendo os dados das transações, com colunas limpas e formatadas.
As colunas incluem: \code{data}, \code{descricao}, \code{valor}, \code{tipo}, \code{id_transacao} e \code{memo}.
}
\description{
Esta função utiliza a biblioteca Python \code{ofxparse} através do pacote \code{reticulate}
para ler e analisar arquivos OFX. Esta abordagem é mais robusta do que depender
de pacotes R que podem não estar atualizados ou disponíveis para a sua versão do R.

A função lida com a dependência do Python automaticamente. Na primeira vez que
for executada, ela verificará se o \code{ofxparse} está instalado no ambiente
reticulate e, se não estiver, irá instalá-lo. Isso pode levar um momento.
As execuções subsequentes serão mais rápidas.
}
\examples{
\dontrun{
caminho <- "caminho/para/seu/extrato.ofx"
transacoes_tbl <- importar_dados_ofx(caminho)
print(transacoes_tbl)
}
}
